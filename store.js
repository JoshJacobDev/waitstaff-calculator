const subtotal = 0;
const tipAmount = 0;
const total = 0;

const earnings = {
    tipTotal: 0,
    mealCount: 0,
    average: 0
}

export default {
    subtotal,
    tipAmount,
    total,
    earnings
}